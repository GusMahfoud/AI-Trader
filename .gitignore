# Model checkpoints (large binaries)
*.pt

# Python cache + build artefacts
__pycache__/
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/

# IDE
.vscode/

# Cursor project config (holds MCP access tokens) + local secrets
.cursor/
.env
!.env.example

# macOS
.DS_Store

# Scratch/archive directories
runs/
archived_results/

# Per-run results (large files) and local experiment overrides
results/

# Private working docs (plans, metrics guide, paper-trading manual + results)
docs/
CLAUDE.md

# Personal paper-trading records (written by --mode picks)
paper_trading/

# Downloaded market data / parquet cache (root-level only — NOT src/ai_trader/data!)
/data/