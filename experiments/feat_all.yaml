# Tier 4 (features) — add all three regime features to the champion (upper-bound test).
# Everything else inherits the champion base.
env:
  extra_features: ["volume_regime", "vol_regime", "price_position"]
