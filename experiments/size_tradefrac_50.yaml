# Tier 1 (sizing) — larger per-trade step: 50% of equity (champion uses 25%).
# Faster ramp to full exposure; tests sensitivity to trade granularity.
# One variable changed from the champion base.
env:
  trade_fraction: 0.50
